PointLook Webserver using nginx
=====================
www contains the /home/www data owned by www-data. 
Default web root is /home/www/html
look at sample to use

Docker images built using ``` apt-get install docker-engine```
